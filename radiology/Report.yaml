# Report schema
# Defines the radiology report text payload for use in DCR extension payloads.

Report:
  type: object
  required:
    - reportText
  properties:
    reportText:
      type: string
      description: The radiology report text.
      example: "No acute cardiopulmonary abnormality."
